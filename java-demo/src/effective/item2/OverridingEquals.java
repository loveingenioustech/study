package effective.item2;

public class OverridingEquals {
	/*
	 * 
	 * 
	 * • Reflexive: For any non-null reference value x,x.equals(x)must return
	 * true. • Symmetric: For any non-null reference values xand
	 * y,x.equals(y)must returntrueif and only if y.equals(x)returns true. •
	 * Transitive: For any non-null reference values x,y,z, if
	 * x.equals(y)returns trueand y.equals(z)returns true, then x.equals(z)must
	 * return true. • Consistent: For any non-null reference values xand y,
	 * multiple invocations ofx.equals(y)consistently return trueor consistently
	 * return false, provided no information used in equalscomparisons on the
	 * objects is modified. • For any non-null reference value
	 * x,x.equals(null)must return false.
	 */

}
